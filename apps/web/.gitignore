# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# MediaPipe wasm 런타임 — 생성물이다. `scripts/copyMediapipeWasm.js`가 매 dev/build 앞에서
# npm 패키지(`@mediapipe/tasks-vision`)로부터 다시 복사한다. 커밋하면 패키지 버전이 올라갈 때
# wasm만 옛 버전으로 남고, 그 불일치는 런타임에서만 드러난다(스크립트 상단 주석 참고).
#
# `public/models/`는 **일부러 여기 넣지 않는다** — 모델 .tflite는 npm에 없고 GCS 배포본을
# 받아야 해서, 빌드 스텝으로 만들면 빌드가 네트워크에 의존하게 된다. 재생성할 원본이 없는
# 자산은 저장소가 원본이어야 낡지 않는다.
public/mediapipe/

# mkcert가 만든 dev 서버 HTTPS 인증서 — **개인 키다. 절대 커밋하지 않는다.**
# iOS 실기기에서 dev 서버로 세션 화면을 띄울 때만 쓴다(`vite.config.ts`의 `devHttps` 주석).
# 인증서에는 발급 시점의 LAN IP가 박히므로 개발자·네트워크마다 달라 공유할 값도 아니다.
.certs/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

/**
 * Vision 파이프라인의 튜닝 상수.
 *
 * `ai-wiki/product/mvp-scope.md`가 "하드코딩하지 말고 설정 파라미터로 구현"하라고 명시한
 * 값들이 여기 모인다 — M1 테스트에서 실측으로 조정된다(설계 문서 §12).
 *
 * 이 파일에는 **모델·추론 상수도 함께 들어온다**(프레임 주기·score 임계).
 */

/**
 * `getUserMedia` 제약.
 *
 * 모델 입력은 320×320이라 해상도는 **프리뷰 화질용**이다(설계 §3). 낮으면 풀스크린
 * 프리뷰가 뭉개지고, 높으면 배터리·발열이 늘어난다.
 * `ideal`을 쓰는 이유는 지원하지 않는 기기에서 `getUserMedia`가 실패하지 않게 하기 위해서다.
 *
 * ⚠️ **이 값은 요청일 뿐 실제로 받는 값이 아니다 — 2026-07-30 실측으로 확인됐다.**
 *
 * | 환경                        | 요청      | 실제 수신           |
 * | --------------------------- | --------- | ------------------- |
 * | iPhone 17 Pro (세로)        | 720×1280  | **1280×720**        |
 * | iPhone 17 Pro (가로)        | 720×1280  | **720×1280**        |
 * | Android 에뮬레이터 (전면)   | 720×1280  | **1280×720**        |
 * | Android 에뮬레이터 (후면)   | 720×1280  | **960×720**         |
 *
 * `ideal`은 소프트 제약이라 기기가 무시해도 되고, 실제로 무시한다. 9:16은 센서의 네이티브
 * 비율이 아니라서 더 그렇다. **프레임 비율에 의존하는 코드를 쓰지 말 것** — 실제 값은
 * `visionDiagnostics.cameraStream`(`?diag=1`)으로 확인한다.
 *
 * ⚠️ **프레임 비율이 화면 비율과 다르면 어떤 표시 방식으로도 손해가 난다**(자르거나 `cover`,
 * 남기거나 `contain`, 찌그러뜨리거나 `fill`) — 실기기에서 그 차이가 74%였다(자세한 내용과
 * 현재 선택은 `CameraPreviewSurface.tsx` 주석 참고).
 *
 * ## ❌ `aspectRatio` 요청은 시도했고 **되돌렸다** (BY-336, 2026-08-01 실측)
 *
 * "화면 비율을 `aspectRatio: { ideal }`로 함께 요청하면 어긋남을 원천에서 줄일 수 있다"는
 * 아이디어를 실제로 구현해 실기기에서 확인했다. **결과는 더 나빴다** — 전면 세로에서 화각이
 * 눈에 띄게 좁아졌다(“너무 크롭해 보인다”).
 *
 * 원인: 브라우저는 이 제약을 **센서 판독 방향을 바꿔서**가 아니라 **프레임을 잘라서** 만족시킨다.
 * 세로 화면 비율(≈0.46)을 요청하면 가로로 들어오는 센서 프레임의 좌우를 크게 도려낸 뒤 주므로,
 * `object-fit`이 잘라내던 것을 **카메라가 미리 잘라 주는** 꼴이 된다. 화면에 보이는 잘림은
 * 그대로인데 원본 화각만 줄어든다 — 순손해다.
 *
 * **다시 시도하지 말 것.** 회전 시 재오픈(`CameraAdapter.reopen`)도 이 요청을 위해 있던 것이라
 * 함께 걷어냈다. 방향에 따른 잘림은 표시 단계(`object-fit`·프리뷰 영역 크기)에서만 다룬다.
 */
export const CAMERA_CONSTRAINTS = {
  front: {
    video: { facingMode: "user", width: { ideal: 1280 } },
    audio: false,
  },
  back: {
    video: { facingMode: "environment", width: { ideal: 1280 } },
    audio: false,
  },
} as const satisfies Record<"front" | "back", MediaStreamConstraints>;

/* ------------------------------------------------------------------ *
 * 모델 · 추론 (설계 문서 §2·§3·§4)
 * ------------------------------------------------------------------ */

/**
 * MediaPipe wasm 런타임 디렉터리.
 *
 * `apps/web/public/`에 두면 `dist`를 거쳐 앱 번들까지 그대로 흘러간다
 * (`apps/mobile/scripts/syncWebDist.js`가 재귀 전체 복사). 그래서 브라우저(`localhost:5173`)와
 * WebView(`http://127.0.0.1:{포트}`) 양쪽에서 같은 절대 경로가 통한다.
 *
 * 이 디렉터리는 손으로 채우지 않는다 — `scripts/copyMediapipeWasm.js`가 npm 패키지에서
 * 복사한다. 손으로 넣으면 패키지 버전이 올라갈 때 wasm만 옛 버전으로 남는다.
 */
export const MEDIAPIPE_WASM_PATH = "/mediapipe/wasm";

/** 번들에 동봉하는 EfficientDet-Lite0 변형. 설계 §2가 둘 다 넣고 실측 비교하라고 정했다. */
export const MODEL_PATHS = {
  int8: "/models/efficientdet_lite0_int8.tflite",
  fp32: "/models/efficientdet_lite0_fp32.tflite",
} as const;

export type ModelVariant = keyof typeof MODEL_PATHS;

/**
 * 기본 변형 — **int8** (BY-305 발열 실측으로 fp32에서 전환, 2026-08-14).
 *
 * 원래 fp32였다 — 저조도에서 `person`을 놓치는 정도가 곧 순공시간 오차라, 크기만 보고
 * int8을 성급히 고르지 않는다는 이유였다(설계 §2). BY-305 실기기 QA(iPhone 17 Pro)에서
 * CPU 추론이 발열 단독 주범으로 확정되어(`FRAME_INTERVAL_MS` 주석의 실측 참고) 프레임당
 * 비용이 절반인 int8(S3 실측 213~371ms vs fp32 401~594ms)로 내린다.
 *
 * ⚠️ **int8만으로는 발열이 잡히지 않는다 — 주기와 짝이다.** 추론이 주기보다 길면 busy-guard
 * 백투백 포화로 듀티가 ~100%에 머물기 때문에, 모델을 빠르게 하는 것만으로는 유휴가 생기지
 * 않는다. 거꾸로 int8이 있어야 당시 주기(500ms)에서 유휴가 생겼다 — fp32(401~594ms)였다면
 * 500ms에서도 여전히 포화다. 둘은 대체재가 아니라 순서가 있는 조합이다. 지금 주기(1000ms)에서는
 * fp32도 포화는 아니지만 듀티가 int8의 두 배(~40~60%)다.
 *
 * ⚠️ 설계 §2가 요구한 저조도 정확도 비교는 아직 안 됐다 — BY-304에서 검증하고, 순공
 * 오차가 유의미하면 fp32로 되돌린 뒤 주기 확대만으로 발열을 잡는다(듀티 ~30% → ~50%).
 */
export const DEFAULT_MODEL_VARIANT: ModelVariant = "int8";

/** COCO 80클래스 중 우리가 알아야 할 둘. 나머지는 후처리 비용일 뿐이다(설계 §2). */
export const CATEGORY_ALLOWLIST = ["person", "cell phone"] as const;

/**
 * 라벨별 score 임계 — **M1 튜닝 대상**(mvp-scope.md 감지 파라미터).
 *
 * `person`을 낮게 두는 이유는 오탐의 방향 때문이다. 고개를 숙이거나 거치 각도 때문에
 * 놓치면 **공부 중인데 순공에서 빠진다** — 자리 이탈 오탐은 사용자가 실제로 한 공부를
 * 지우는 결과가 된다(설계 §4).
 *
 * ⚠️ MediaPipe `ObjectDetector`는 **임계를 하나만** 받는다. 그래서 검출기에는 둘 중 낮은
 * 값을 주고(`DETECTOR_SCORE_THRESHOLD`), 라벨별 구분은 `detectionRules`가 한 번 더 거른다.
 * 검출기 임계를 높게 주면 그 아래 검출은 애초에 배열에 오지 않아 규칙이 손댈 수 없다.
 *
 * ## person 0.3 (2026-09-22, 0.4에서 되돌림)
 *
 * 2026-09-21에 1 fps 전환과 함께 0.4·0.5를 시도했다가 0.4에 뒀는데, 2026-09-22 졸음 측정에서
 * 꾸벅거리며 고개를 떨구는 동안 person 점수가 하위 5% 0.45까지 내려왔다. 1 fps에서는 프레임
 * 하나의 미검출이 1000ms 유지되어 `AWAY.enterMs` 2000ms 안에 두 번만 놓치면 이탈로 찍히므로,
 * 0.4는 그 자세에서 이탈 오탐과 0.05 차이였다. 원래 값 0.3으로 되돌린다 — 위 원칙(자리 이탈
 * 오탐은 실제로 한 공부를 지운다)이 이기는 방향이다. 발열·배터리와는 무관한 값이다(후처리 임계).
 */
export const SCORE_THRESHOLDS = {
  person: 0.3,
  phone: 0.4,
} as const;

/** 검출기에 넘길 임계 — 라벨별 임계 중 최솟값. 위 주석 참고. */
export const DETECTOR_SCORE_THRESHOLD = Math.min(SCORE_THRESHOLDS.person, SCORE_THRESHOLDS.phone);

/**
 * 프레임 처리 주기 — 1000ms(1 FPS).
 * (BY-305, 2026-08-19에 200ms→500ms · 2026-09-21에 500ms→1000ms로 확대)
 *
 * ## 왜 200ms를 떠났나
 *
 * 200ms는 **명목값일 뿐 실효 주기가 아니었다.** 추론이 그보다 오래 걸려서(fp32 401~594ms,
 * int8 213~371ms) `frameLoop`의 busy-guard가 매 tick 프레임을 버리고, 추론은 끝나는 즉시
 * 다음이 시작되는 **백투백 루프로 돌았다 — CPU 듀티 ~100%.** 실효 샘플 간격을 정하는 건 이
 * 상수가 아니라 추론 시간이었다.
 *
 * 그 대가가 발열이다. iPhone 17 Pro 실기기에서 세션 16분 만에 thermal state Nominal→Serious,
 * 일시정지 10분(카메라·프리뷰·세션 틱 유지, 추론만 정지)에서는 Fair→Nominal로 오히려 식었다
 * — 발열의 단독 주범은 카메라가 아니라 추론이다.
 *
 * ## 주기는 `PHONE.enterMs`와 짝이다 — 500ms에 머문 이유와 1000ms로 간 이유
 *
 * BY-305 당시 1000ms도 시도했지만(듀티 ~30%) 500ms에 머물렀다. **PHONE의 확정 유지시간
 * (`enterMs`, 당시 500ms)보다 샘플링 간격이 길어지면**, 그보다 짧은 폰 사용이 두 샘플 사이에
 * 통째로 들어가 어느 프레임에도 걸리지 않기 때문이다 — 지연이 아니라 **누락**이고, 비집중이
 * 집중으로 계산되어 순공시간이 과대 집계된다(`mvp-scope.md`의 "측정할 수 없는 시간은 집중으로
 * 인정하지 않는다"와 반대 방향). 주기는 `enterMs`를 넘을 수 없고, 500ms가 그 경계값이었다.
 *
 * 2026-09-21에 발열·배터리를 더 줄이기 위해 **`PHONE.enterMs`를 1000ms로 함께 올리고** 주기를
 * 1000ms로 확대했다(`../detection.ts`의 `DEFAULT_DETECTION_PARAMS`). 경계 규칙은 그대로다 —
 * 주기가 `enterMs`와 같으므로 신호가 두 샘플 사이로 사라지는 구간이 생기지 않는다. 그 대가로
 * 1초 미만의 폰 사용은 잡지 않는다. 500ms 시절에도 0.5초 미만은 잡지 않았으므로 원칙이 바뀐 게
 * 아니라 문턱이 올라간 것이고, 이건 발열을 잡기 위해 감지 반응성을 내준 제품 결정이다.
 *
 * 기대 효과는 int8 실측(213~371ms) 기준 CPU 듀티 ~43~74% → **~21~37%**다. 이 상수 외의
 * 값(score 임계·유지시간)은 후처리라 발열과 무관하다 — 발열 레버는 이 주기 하나다.
 *
 * ## 판정에 미치는 영향 (`../detection.ts`)
 *
 * 유지시간 디바운스는 벽시계 기반이고 200ms 세션 틱마다 평가되므로, 신호가 샘플 사이에
 * 유지되는 동안에도 시간은 계속 누적된다. 그래서 확정에 필요한 실제 유지 시간은
 * **관측 지연(0~1000ms) + `enterMs`**다 — PHONE(1000ms)은 1.0~2.0초(평균 ~1.5초),
 * AWAY(2000ms)는 2.0~3.0초. DEVICE는 가속도 센서 경로(20Hz)라 이 주기와 무관하다.
 *
 * AWAY의 `enterMs`를 주기의 2배로 둔 이유는 샘플 하나의 무게 때문이다. 1 fps에서는 person을
 * 한 프레임 놓치면 그 미검출이 1000ms 동안 유지된다. 2000ms면 연속 두 프레임을 놓쳐야 이탈로
 * 확정되므로 고개를 숙이는 등의 순간 미검출은 걸러진다.
 *
 * 반대 방향 오차도 있다 — 한 샘플에만 걸린 짧은 신호는 다음 샘플까지 참으로 유지되어 실제보다
 * 길게 집계된다(최대 1000ms). 지연·누락·과대 집계는 서로 다른 문제이므로 BY-304에서 각각
 * 측정한다.
 *
 * ⚠️ **저사양 기기는 미검증이다.** 추론이 1000ms를 넘으면 다시 포화되어 이 값의 효과가 사라진다
 * (A13급에서 실측 필요 — `WebKit.WebContent` 프로세스 CPU%로 확인한다). 500ms 시절보다 여유는
 * 두 배다.
 */
export const FRAME_INTERVAL_MS = 1000;

/**
 * delegate 시도 순서 — **CPU 하나뿐이다.**
 *
 * 설계 §2는 `GPU` 우선에 실패 시 CPU 폴백으로 시작했다. S3 실측(2026-07-29, Android
 * 에뮬레이터 Pixel 3 / API 35 WebView)에서 그 기본값을 뒤집었다.
 *
 * **GPU delegate가 조용히 틀린 값을 낸다.** 같은 프레임에서:
 *
 * | delegate | 검출 결과            | 프레임당      |
 * | -------- | -------------------- | ------------- |
 * | GPU      | person **1,648건**, 전부 정확히 score 0.50 | 604~800ms |
 * | CPU      | **0건** (정답)       | 401~594ms     |
 *
 * int8도 같다(GPU 2,775건 / CPU 0건). 이 쓰레기가 그대로 `personPresent = true`가 되어
 * **앱이 영원히 "집중 중"이라고 보고한다** — 실제로 세션을 2분 42초 돌려 재현했다.
 * 집중률 100%가 그대로 서버에 저장되므로, `mvp-scope.md`의 "측정할 수 없는 시간은 집중으로
 * 인정하지 않는다"를 정면으로 어긴다. **느린 것보다 훨씬 나쁘다.**
 *
 * GPU를 폴백으로도 두지 않는 이유: 폴백은 "실패했을 때" 발동하는데 GPU는 실패하지 않고
 * **성공한 얼굴로 틀린 답을 준다.** 잡을 신호가 없다.
 *
 * ⚠️ **에뮬레이터 1종에서만 관측했다.** 실기기 GPU에서는 정상일 수 있고, 그쪽이 2.7배
 * 빠르므로 되돌릴 값어치가 있다. 실기기 확보 후 같은 비교를 다시 하고, 정상이면 이 배열만
 * `["GPU", "CPU"]`로 돌린다(설계 문서 §10 S3 결과 참조).
 */
export const DELEGATE_ORDER = ["CPU"] as const;

/** 폴백 로직이 다루는 전체 집합. `DELEGATE_ORDER`가 그중 무엇을 실제로 시도할지 정한다. */
export type Delegate = "GPU" | "CPU";

/* ------------------------------------------------------------------ *
 * 졸음 감지 · 얼굴 모델
 * ------------------------------------------------------------------ */

/**
 * MediaPipe Face Landmarker 모델.
 *
 * 출처 https://storage.googleapis.com/mediapipe-models/face_landmarker/face_landmarker/float16/1/face_landmarker.task
 * sha256 64184e229b263107bc2b804c6625db1341ff2bb731874b0bcc2fe6544e0bc9ff, 3,758,596 바이트.
 * `.tflite`와 같은 이유로 커밋한다 — npm에 없고, 빌드 스텝으로 받으면 빌드가 네트워크에
 * 의존하며 원격 파일이 바뀌어도 알 수 없다. `scripts/copyMediapipeWasm.js` 주석 참고.
 */
export const FACE_MODEL_PATH = "/models/face_landmarker.task";

/**
 * 얼굴 모델 생성 옵션.
 *
 * `numFaces`가 1인 것은 비용 때문이고, 그 대가로 여러 사람이 찍히면 모델이 고른 한 명만 본다
 * (다중 인물은 `detectionRules.ts`의 자리 이탈 한계와 같은 부류다).
 *
 * 검출·존재 신뢰도는 0.6에서 0.5(기본값)로 내렸다(2026-09-22 실측). 고개를 17° 숙이고 눈을 감은
 * 구간에서 얼굴이 30번 중 10번 빠져, 눈 점수는 감김으로 읽히는데 10초 유지가 끊겨 졸음이 서지
 * 않았다. 얼굴이 빠지면 "판정 없음"이라 오탐 방향이 아니고, 신뢰도를 내려 얼굴을 더 자주 잡는 것은
 * 놓치는 쪽만 줄인다. 추적 신뢰도는 그대로 0.6이다 — 프레임 사이 추적은 끊겨도 다음 검출이 받는다.
 */
export const FACE_LANDMARKER_OPTIONS = {
  numFaces: 1,
  minFaceDetectionConfidence: 0.5,
  minFacePresenceConfidence: 0.5,
  minTrackingConfidence: 0.6,
} as const;

/**
 * 얼굴 추론을 도는 간격 — 처리된 프레임 2개마다 한 번, 즉 2초.
 *
 * **이 값은 `FRAME_INTERVAL_MS`와 짝이다.** 아래 `*_SAMPLES` 상수들이 전부 "얼굴 틱 2초"를
 * 전제로 잡혀 있어서(보정 30초·비율 창 44초·깨어남 6초), 얼굴 틱이 바뀌면 그 시간이 전부
 * 같이 늘어난다. 2026-09-21에 프레임 주기가 500ms→1000ms로 늘면서 4→2로 내려 얼굴 틱 2초를
 * 유지했다. `__tests__/visionConfig.test.ts`가 비율 창 44초를 고정해 이 짝이 깨지면 잡는다.
 *
 * 발열 예산의 손잡이다. 추가 CPU 듀티는 얼굴 추론 시간 ÷ (이 값 × `FRAME_INTERVAL_MS`)이고,
 * iPhone 17 실측 44~54ms에서 얼굴 틱 2초면 +2.5%p다. 유지시간이 10초 이상이라 2초 간격에서도
 * 표본이 5개 이상 쌓인다. 앱 안 발열 측정에서 모자라면 3, 4로 올린다(얼굴 틱 3초·4초).
 */
export const FACE_FRAME_DIVISOR = 2;

/**
 * 모델의 blendshape 헤드에서 읽는 이름. 타입 정의에 없고 런타임에 온다.
 * 2026-09-20 실측에서 52개 중 이 넷이 모두 존재하는 것을 확인했다.
 */
export const FACE_BLENDSHAPE_ALLOWLIST = [
  "eyeBlinkLeft",
  "eyeBlinkRight",
  "eyeLookDownLeft",
  "eyeLookDownRight",
] as const;

export type EyeBlendshapeName = (typeof FACE_BLENDSHAPE_ALLOWLIST)[number];

/**
 * 없으면 눈 판정을 포기하는 이름. 판정에 실제로 쓰는 둘뿐이다.
 *
 * `eyeLookDown*`은 내려다봄 거부권을 걷어낸 뒤로 판정에 쓰이지 않고 진단에만 실린다. 그것까지
 * 필수로 걸면 그 이름만 바뀌어도 `eyeBlink*`가 멀쩡한 채 졸음 감지가 세션 내내 침묵한다.
 */
export const FACE_BLENDSHAPE_REQUIRED = ["eyeBlinkLeft", "eyeBlinkRight"] as const;

export type RequiredEyeBlendshapeName = (typeof FACE_BLENDSHAPE_REQUIRED)[number];

/** 눈 바깥 꼬리 랜드마크 인덱스(478점 메시 기준). 품질 게이트 계산에만 쓰고 좌표는 나가지 않는다. */
export const EYE_OUTER_CORNER_LANDMARKS = { left: 33, right: 263 } as const;

/**
 * 고개 숙임 각도(카메라 기준, 아래가 양수)가 이 값 이상이면 눈 판정을 하지 않는다 — **내려다봄 게이트.**
 * `HEAD_PITCH_MEDIAN_SAMPLES`틱 중앙값으로 본다. **잠정값이다.**
 *
 * ## 왜 절대 각도인가
 *
 * 오탐이 나는 조건은 하나다 — 카메라가 시선보다 위에 있어 윗눈꺼풀이 눈을 가려 보이는 것. 그것이 곧
 * 카메라 기준 고개 각도다. 아래에서 찍으면 각도가 음수라 감지 구간에 들어오고 눈이 보이니 감김도
 * 정확하다. 정면에서 찍으면 책·폰을 볼 때 18° 이상으로 올라가고, 위에서 찍으면 거의 항상 이 값을
 * 넘어 감지를 하지 않는다(리더 결정: 위에서 찍는 배치는 감지 포기).
 *
 * ## 왜 "시작 순간의 움직임"이 아니라 "지금 각도"인가
 *
 * 2026-09-22 BY-704 6~9회차에서 특허(US 11144756) 방식 — 감김이 시작될 때 고개가 움직였으면 시선 이동
 * — 을 구현해 봤다. 원리는 맞지만 **눈을 감은 표본의 각도는 믿을 수 없었다.** 얼굴 모델이 눈을 잃으면
 * 자세 추정이 흔들려, 감은 동안 5회차는 ±1.5°였는데 8회차는 최대 23°, 9회차는 -23~+18°를 오갔다.
 * 시작 순간의 각도를 보는 규칙은 그 흔들림을 시선 이동으로 읽어 진짜 감김을 잠갔고, 이를 메우려는
 * 상태(쉬는 자세·잠금·계속·급강하)가 회차마다 다른 데서 깨졌다. 상태 없이 매 틱 "지금 고개가 이
 * 값 아래인가"만 보면 흔들리는 동안은 놓칠 뿐 오탐은 없다. 리더 우선순위: 오탐 0 > 놓침.
 *
 * ## 값의 근거
 *
 * 앉아서 눈을 감을 때 각도 중앙값은 0~5°(2·5·6·9회차), 오탐이 난 자세는 18° 이상(1·2·5회차)이었다.
 * 15°는 양쪽에 여유가 있다. 감은 채 고개가 이 값 넘게 떨어지면 그때부터 세지 않는다 — 허용된 놓침이다.
 * 가운데 구간(15~20°)에 "30초 무깜빡임 = 잠" 규칙을 두는 안은 뺐다. 2초 표본으로는 깜빡임이 안 잡히고,
 * 초당 5~10번 보면 발열이 27~54%p 늘며, 집중해 읽을 때 30초 넘게 안 깜빡이는 것이 흔해 오탐 0이 안 된다.
 */
export const HEAD_PITCH_DOWN_DEG = 15;

/** 게이트가 보는 최근 각도 표본 수. 홀수여야 중앙값이 실제 관측값이다. 한 틱 튐(7회차 18.9°)을 거른다. */
export const HEAD_PITCH_MEDIAN_SAMPLES = 3;

/**
 * 눈 윤곽 16점씩. MediaPipe Face Mesh의 눈 테두리 번호이고 판정에는 쓰지 않는다.
 * 실기기 측정 도구가 화면 위에 눈 자리를 그릴 때만 읽는다. 좌표는 그 캔버스에서 끝난다.
 */
export const EYE_OUTLINE_LANDMARKS = {
  left: [33, 7, 163, 144, 145, 153, 154, 155, 133, 173, 157, 158, 159, 160, 161, 246],
  right: [362, 382, 381, 380, 374, 373, 390, 249, 263, 466, 388, 387, 386, 385, 384, 398],
} as const;

/**
 * 정규화 가로 눈 간격의 최소값. 이보다 작으면 눈 판정을 하지 않는다.
 *
 * 실측에서 정상 거리는 0.11~0.17이었고, 약 1m 떨어지면 0.06까지 떨어지면서 얼굴 검출 자체가
 * 35%로 흔들렸다. 그 구간을 판정에 넣지 않는다.
 */
export const MIN_INTER_OCULAR_NORMALIZED = 0.07;

/**
 * 뜬 눈 기준을 재는 창의 표본 수 — 얼굴 틱 2초 × 15 = 30초. **잠정값이다.**
 *
 * 개인별 보정 연구는 눈을 뜬 채 5초를 재라고 시킨다. 우리는 사용자에게 아무것도 시키지 않고
 * 공부하는 동안 지나가며 재므로 더 길게 본다. 첫 창이 찰 때까지는 눈 판정을 쉰다. 그 사람의
 * 뜬 눈이 몇 점인지 모르는 채로 고정값을 쓰면 뜬 눈이 높은 사람이 시작 직후 졸음으로 찍힌다.
 *
 * 이 창은 세션당 한 번이 아니라 **찰 때마다 다시 돈다**. 근거는 `eyeCalibration.ts` 참고.
 */
export const EYE_CALIBRATION_SAMPLES = 15;

/**
 * 뜬 눈 기준으로 고를 백분위. **문헌이 아니라 판단이고, 창들 사이의 최솟값 설계와 짝이다.**
 *
 * 문헌은 눈을 뜨라고 시킨 뒤 평균을 쓴다. 우리는 시키지 않으므로 보정 중에 깜빡이거나 잠깐
 * 감을 수 있는데, **그 오염을 거르는 일은 이제 창들 사이의 최솟값이 한다** — 한 창 안에서 감김이
 * 기준을 밀어 올려도 다른 창의 낮은 값이 이긴다. 그래서 창 안에서까지 낮은 쪽을 볼 이유가 없다.
 *
 * 오히려 낮은 백분위는 약점이 된다. 0.2면 15표본 중 3개만 낮게 읽혀도 그 창의 기준이 내려가고,
 * 최솟값이라 그 값이 세션 끝까지 붙든다. 고개를 잠깐 젖혀 천장을 보는 것만으로 그 사람의 기준이
 * 영구히 무너진다. 0.4면 6개, 30초 중 12초가 낮아야 움직인다.
 */
export const EYE_CALIBRATION_PERCENTILE = 0.4;

/**
 * 뜬 눈 기준에서 감김으로 보기까지의 간격. **잠정값이다.**
 *
 * 문헌의 개인화는 눈 종횡비에 대해 기준의 75% 아래를 감김으로 보는데, 블렌드셰이프는 0이 뜸이고
 * 1이 감김이라 비율이 아니라 간격으로 옮긴다. 스파이크에서 뜬 눈이 0.18~0.24이고 그때 쓰던 고정
 * 임계가 0.45였으니, 그 피험자에게 같은 임계가 나오는 간격이다.
 */
export const EYE_CALIBRATION_DELTA = 0.25;

/**
 * 임계에 하한과 상한을 두지 않는다. 임계는 언제나 뜬 눈 기준값 + `EYE_CALIBRATION_DELTA`다.
 *
 * 상한을 두지 않는 이유는 방향이다. 뜬 눈이 0.5로 읽히는 사람은 감으면 0.8~0.9까지 갈 수 있는데,
 * 상한으로 자르면 뜬 눈과 임계의 간격이 얇아져 흔들리는 순간 깨어 있는데 졸음으로 찍힌다.
 *
 * 하한은 한때 고정 임계 0.45로 뒀다가 2026-09-20 실기기 측정 뒤 뺐다. 하한이 막던 것은 책을
 * 내려다볼 때 뜬 눈 점수가 오르는 경우다(실측 기준값 0.09, 내려다볼 때 상위 5% 0.41). 하한 없이
 * 기준값 0.09에 0.25를 더하면 임계 0.34라 그 자세가 임계를 넘을 수 있다. 그 위험을 알고도 눈이 큰
 * 사람의 감김(감아도 0.45 아래)을 놓치지 않는 쪽을 골랐다. 공부 중 오탐이 실측에서 나오면
 * `EYE_CALIBRATION_DELTA`를 올리거나 하한을 되살린다.
 */

/**
 * 감김 비율을 재는 창의 표본 수 — 얼굴 틱 2초 × 22 = 44초.
 *
 * PERCLOS는 1994년 정의부터 1분 창이 표준이다. 2026-09-20 실측에서 1분은 꾸벅거림 진입이
 * 늦게 느껴져 45초 근처로 줄였고, 표본이 2초 단위라 44초다. 창이 짧을수록 깨어 있는 사람이
 * 우연히 절반을 넘기기 쉬운데, 그 방향은 `EYE_AWAKE_CLEAR_SAMPLES`가 막는다. 깨어난 뒤의
 * 꼬리도 같은 상수가 끊으므로 창 길이는 진입 속도만 정한다.
 */
export const EYE_RATIO_WINDOW_SAMPLES = 22;

/**
 * 비율 창을 비우는 연속 뜬 눈 표본 수 — 얼굴 틱 2초 × 3 = 6초.
 *
 * 비율 창은 과거를 들고 있어서, 오래 감았다 뜨면 감긴 표본이 밀려 나갈 때까지 졸음을 붙잡는다.
 * 2026-09-20 실측에서 그 꼬리가 최대 33초였고 한 구간은 63초를 졸음에 머물렀다. 6초 동안 눈을
 * 뜨고 있는 사람은 깨어 있는 것이므로, 그때는 창에 무엇이 있든 비운다. 3초 감고 1초 뜨는
 * 꾸벅거림은 2초 표본에서 뜬 눈이 세 번 이어질 수 없어 그대로 잡힌다.
 *
 * 눈 판정이 연속으로 걸러졌을 때 창을 버리는 횟수도 이 값이다. 그만큼 눈을 못 봤으면 창에
 * 남은 것은 지금을 설명하지 못하는 과거라는 근거가 같다.
 */
export const EYE_AWAKE_CLEAR_SAMPLES = 3;

/**
 * 창 안에서 감겨 있어야 하는 비율. **잠정값이다.**
 *
 * 검증된 졸음 문턱은 0.15인데 그것은 주의력 검사 실패와 상관이 높은 피로 수준이지 잠이 아니다.
 * 우리는 잠을 잡으므로 1분 중 30초 이상 감김으로 잡는다. 3초 감고 1초 뜨는 꾸벅거림은 0.75라
 * 잡히고, 책을 보며 눈꺼풀이 무거운 정도는 보정된 임계 아래라 세어지지 않는다. 0.7이 더
 * 보수적인 대안이고 실기기에서 둘을 비교한다.
 */
export const EYE_RATIO_THRESHOLD = 0.5;

/**
 * 눈 감김을 다듬는 창의 크기.
 *
 * 둘이면 짝수라 아래쪽 중앙값, 즉 둘 중 작은 값을 쓴다. 최근 두 표본이 모두 감겨야 감김이고
 * 뜬 표본이 하나만 들어와도 바로 뜬 것이다. 진입은 감김 둘(4초)에 유지시간 10초, 해제는 뜬
 * 표본 하나(최대 2초)에 유지시간 2초라 체감 2~4초다.
 *
 * 셋일 때는 뜬 표본이 둘 들어와야 중앙값이 내려와 해제가 5~7초였고, 2026-09-20 실측에서
 * 그것이 늦게 느껴졌다. 대가는 자는 동안 모델이 한 표본만 뜬 눈으로 잘못 읽으면 풀렸다가
 * 10초 뒤 다시 들어가는 것인데, 그 방향은 졸음을 덜 세는 쪽이지 깨어 있는 사람을 졸음으로
 * 잡는 쪽이 아니다. 깜빡임 한 표본으로 진입하지 않는 것은 그대로다.
 */
export const FACE_SMOOTHING_SAMPLES = 2;

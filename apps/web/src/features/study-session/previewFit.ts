/**
 * 프리뷰가 카메라 프레임을 화면에 맞추는 방식.
 *
 * **컴포넌트 파일이 아니라 여기 두는 이유**: 컴포넌트 파일에서 상수를 함께 export하면
 * **Fast Refresh가 깨진다**. 세션 화면은 dev 서버 HMR로 반복 개발하므로
 * (`apps/mobile/lib/devWebOrigin.ts`) 그 손실이 크다.
 */

/**
 * 표시 방식.
 *
 * - `cover`: 화면을 채우고 넘치는 만큼 자른다. 여백이 없는 대신 **원본 화각의 일부만 보인다** —
 *   실기기 실측에서 가로 화각의 32%만 화면에 들어와 "확대돼 보인다"는 인상을 준다(배율 자체는
 *   0.99로 등배다. 확대가 아니라 잘라낸 결과다).
 * - `contain`: 프레임 전체를 원본 비율·배율로 보여준다. 잘림이 없는 대신 남는 공간이 생기고,
 *   `object-top`과 함께 쓰면 그 공간이 아래로 모여 세션 UI가 쓴다.
 *
 * 2026-07-30 실측(iPhone 17 Pro·Android 에뮬레이터): 스트림이 항상 뷰포트와 수직이라
 * 두 방식의 차이가 74%로 크게 벌어진다. 자세한 표는 `CameraPreviewSurface.tsx` 주석.
 */
export const PREVIEW_OBJECT_FIT: "contain" | "cover" = "cover";

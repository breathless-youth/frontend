pnpm-lock.yaml
node_modules
dist
build
.expo
.turbo
coverage
.superpowers

# apps/web 빌드 산출물의 복사본(scripts/syncWebDist.js가 채운다) — 생성물이라 검사하지 않는다.
apps/mobile/assets/web-dist

# 설치된 에이전트 스킬 묶음(chore b5e073c) — 외부 배포물이라 우리 포맷 규칙 대상이 아니다. 검사에 넣으면 130개 파일이 실패해 dev CI의 Format check가 늘 빨갛다(2026-09-04 확인).
.claude/skills

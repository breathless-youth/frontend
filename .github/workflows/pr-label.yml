name: PR Auto Label

# 릴리스 노트 분류(.github/release.yml)는 PR 라벨로만 동작하는데 이 저장소는 제목 접두어
# 관례(`[feat] ...`)를 쓴다. 그 접두어를 읽어 대응 라벨을 자동으로 붙여 둘을 잇는다.
on:
  pull_request:
    types: [opened, edited, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  label:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: 제목 접두어로 라벨 부여
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          PR: ${{ github.event.pull_request.number }}
          TITLE: ${{ github.event.pull_request.title }}
        run: |
          # "[feat] BY-1 제목" → "feat". 대괄호를 벗겨 타입만 추출한다.
          type="${TITLE#\[}"
          type="${type%%\]*}"
          case "$type" in
            feat) label=feature ;;
            fix | "!HOTFIX") label=fix ;;
            docs) label=docs ;;
            style | refactor | test | chore | design | comment | rename | remove | build) label=chore ;;
            *) echo "매칭되는 접두어 없음: '$type' — 라벨을 붙이지 않는다"; exit 0 ;;
          esac
          # 라벨이 없으면 --add-label이 실패하므로 먼저 보장한다(--force는 있으면 갱신, 없으면 생성).
          gh label create "$label" --repo "$REPO" --force >/dev/null 2>&1 || true
          gh pr edit "$PR" --repo "$REPO" --add-label "$label"

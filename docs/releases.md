# 릴리즈 기록

앱은 모든 화면을 웹뷰로 열기 때문에 앱 바이너리와 웹 배포가 각자 나간다. 이 문서는 어느 빌드가
어느 채널에 올라가 있고 무엇을 전제로 하는지, 그 조합만 남긴다. 커밋 해시나 빌드 상세 같은 원천
데이터는 EAS 빌드 기록과 Vercel 배포 기록에 있으니 여기에 다시 적지 않는다.

## 기록 규칙

- 스토어 제출, TestFlight·Play 내부 테스트 업로드, dev → main 머지를 한 사람이 그 자리에서
  아래 표에 한 행을 추가한다.
- 앱 빌드는 그 빌드의 커밋에 git 태그를 남긴다. iOS는 `ios/<버전>-<빌드번호>`, Android는
  `android/<버전>-<빌드번호>` 형식이다 (예: `ios/1.0.1-2`).
- 웹은 main 머지 커밋이 곧 릴리즈라서 따로 태그를 달지 않는다.
- 빌드 커밋의 원천은 EAS 기록이고 `cd apps/mobile && npx eas-cli build:list`로 확인한다.
- 비고에는 호환 여부를 판단할 때 필요한 내용만 한 줄로 적는다. 포함하거나 빠진 기능, 지원하는
  capability 정도면 충분하다.
- capability는 셸이 웹에 쿼리로 알려주는 지원 표시를 말한다 (예: `share=1`, `cameraGate=1`).

## 기록

| 날짜       | 컴포넌트 | 버전(빌드) | 커밋      | 채널             | 비고                                                        |
| ---------- | -------- | ---------- | --------- | ---------------- | ----------------------------------------------------------- |
| 2026-08-20 | Android  | 1.0.2(5)   | `c28828a` | Play 내부 테스트 | 심사 통과, 출시 전. 소셜룸·딥링크 미포함                    |
| 2026-08-10 | Android  | 1.0.1(4)   | `a53bbdc` | 확인 필요        | 채널 확인 후 갱신                                           |
| 2026-08-09 | iOS      | 1.0.1(2)   | `99ffe5f` | App Store        | 현행 출시본. 소셜룸·딥링크 미포함, `associatedDomains` 없음 |
| 2026-08-06 | iOS      | 1.0.0(2)   | `b8bfc77` | 확인 필요        | 채널 확인 후 갱신                                           |
| 2026-08-02 | iOS      | 1.0.0(2)   | `889b296` | 확인 필요        | 채널 확인 후 갱신                                           |
| 2026-08-02 | Android  | 1.0.0(2)   | `889b296` | 확인 필요        | 채널 확인 후 갱신                                           |
